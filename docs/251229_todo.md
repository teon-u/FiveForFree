# FiveForFree 학습 로직 개선 TODO

> 작성일: 2024-12-29
> 목적: 학습 관련 코드 구조화 분석 및 개선

---

## 📋 목차

1. [현재 Target 설정](#1-현재-target-설정)
2. [학습 아키텍처 구조](#2-학습-아키텍처-구조)
3. [발견된 문제점](#3-발견된-문제점)
4. [개선 작업 목록](#4-개선-작업-목록)
5. [작업 진행 상황](#5-작업-진행-상황)

---

## 1. 현재 Target 설정

### 1.1 설정값 (config/settings.py)

| 설정 | 값 | 설명 |
|------|-----|------|
| `TARGET_PERCENT` | 0.5 | 목표 수익률 (±0.5%) |
| `PREDICTION_HORIZON_MINUTES` | 60 | 예측 시간 범위 (60분) |
| `PROBABILITY_THRESHOLD` | 0.60 | 신호 발생 최소 확률 |
| `USE_HYBRID_ENSEMBLE` | True | 하이브리드 앙상블 사용 |
| `ENSEMBLE_ALPHA` | 0.5 | 직접 예측 가중치 (50%) |

### 1.2 Target 종류

#### Structure A: 직접 예측 (Primary)
```
PREDICTION_TARGETS = ["up", "down"]
```

| Target | 레이블 조건 | 의미 |
|--------|------------|------|
| `up` | 60분 내 +0.5% 도달 | 상승 예측 |
| `down` | 60분 내 -0.5% 도달 | 하락 예측 |

**레이블 생성 로직 (label_generator.py:126-127):**
```python
label_up = 1 if max_gain >= self.target_percent else 0
label_down = 1 if max_loss <= -self.target_percent else 0
```

#### Structure B: 하이브리드 예측 (Secondary)
```
HYBRID_TARGETS = ["volatility", "direction"]
```

| Target | 레이블 조건 | 의미 |
|--------|------------|------|
| `volatility` | ±0.5% 도달 여부 | 변동성 발생 예측 |
| `direction` | 상승/하락 방향 | 방향 예측 (volatility=1일 때만 유효) |

**레이블 생성 로직 (label_generator.py:150-170):**
```python
# 변동성: up 또는 down 중 하나라도 도달
label_volatility = 1 if (label_up == 1 or label_down == 1) else 0

# 방향: 어느 쪽이 먼저 도달했는지
if label_volatility == 1:
    if minutes_to_target_up <= minutes_to_target_down:
        label_direction = 1  # 상승이 먼저
    else:
        label_direction = 0  # 하락이 먼저
```

### 1.3 최종 예측 결합

```python
# realtime_predictor.py:320-321
final_up_prob = alpha * direct_up_prob + (1 - alpha) * hybrid_up_prob
final_down_prob = alpha * direct_down_prob + (1 - alpha) * hybrid_down_prob
```

**현재 가중치 (alpha=0.5):**
- 직접 예측 (Structure A): 50%
- 하이브리드 예측 (Structure B): 50%

### 1.4 모델별 Target 구조

```
Ticker (예: AAPL)
├── Structure A 모델 (10개)
│   ├── up/
│   │   ├── xgboost_up.pkl
│   │   ├── lightgbm_up.pkl
│   │   ├── lstm_up.pkl + .pt + .scaler
│   │   ├── transformer_up.pkl + .pt + .scaler
│   │   └── ensemble_up.pkl
│   └── down/
│       ├── xgboost_down.pkl
│       ├── lightgbm_down.pkl
│       ├── lstm_down.pkl + .pt + .scaler
│       ├── transformer_down.pkl + .pt + .scaler
│       └── ensemble_down.pkl
│
└── Structure B 모델 (USE_HYBRID_ENSEMBLE=True일 때, 10개 추가)
    ├── volatility/
    │   ├── xgboost_volatility.pkl
    │   ├── ...
    └── direction/
        ├── xgboost_direction.pkl
        ├── ...
```

---

## 2. 학습 아키텍처 구조

### 2.1 학습 파이프라인

```
[데이터 수집]
    │
    ▼
MinuteBar (DB) ──────────────────────────────────────
    │
    ▼
[피처 엔지니어링] FeatureEngineer.compute_features()
    │  - 가격 피처 (15): SMA, RSI, MACD 등
    │  - 변동성 피처 (10): ATR, Bollinger Bands
    │  - 거래량 피처 (8): OBV, MFI
    │  - 모멘텀 피처 (8): ROC
    │  - 시장 컨텍스트 (5)
    │  - 시간 피처 (3)
    │  Total: 49 features
    ▼
[레이블 생성] LabelGenerator.generate_labels()
    │  - label_up, label_down (Structure A)
    │  - label_volatility, label_direction (Structure B)
    ▼
[Train/Val Split] 80/20 시계열 순서 유지
    │
    ▼
[모델 학습] GPUParallelTrainer
    ├── Tree Models (Parallel): XGBoost, LightGBM
    ├── Neural Models (Sequential): LSTM, Transformer
    └── Ensemble (After Base): Meta-learner
    │
    ▼
[모델 저장] ModelManager.save_models()
    │  - .pkl (모델 메타데이터)
    │  - .pt (PyTorch 가중치)
    │  - .scaler (정규화 스케일러)
    │  - .json (XGBoost booster)
```

### 2.2 각 모델의 특성

| 모델 | 입력 형태 | GPU 지원 | 저장 파일 |
|------|----------|----------|----------|
| XGBoost | (n_samples, 49) | ✅ | .pkl, .json |
| LightGBM | (n_samples, 49) | ✅ | .pkl |
| LSTM | (n_samples, 60, 49) | ✅ | .pkl, .pt, .scaler |
| Transformer | (n_samples, 60, 49) | ✅ | .pkl, .pt, .scaler |
| Ensemble | base model predictions | ❌ | .pkl |

---

## 3. 발견된 문제점

### 🔴 Critical (우선 해결)

#### 문제 1: 학습 미완료 상태에서 0% 표시
- **위치**: `base_model.py:201-250`
- **증상**: 대시보드에서 hit_rate가 0%로 표시
- **원인**:
  - `prediction_history`가 비어있으면 `train_accuracy` 반환
  - `train_accuracy`가 0.0이면 0% 표시
  - 모델은 `is_trained=True`인데 정확도 데이터 없음

#### 문제 2: LSTM/Transformer .pt 파일 로드 실패
- **위치**: `model_manager.py:95-100`
- **증상**: 예측 시 "Model not trained" 에러
- **원인**:
  - .pt 파일이 없거나 손상되면 `_model=None`
  - 로드 실패해도 `is_trained=True` 유지
  - `predict_proba()` 호출 시 에러 발생

#### 문제 3: 앙상블 균등 가중치 (25%씩)
- **위치**: `ensemble_model.py:136-159`
- **증상**: 성능 무관하게 모든 모델 25% 가중치
- **원인**:
  - 초기에는 `prediction_history` 없어서 precision=0
  - precision이 BREAKEVEN(0.30) 이하면 균등 분배
  - `train_accuracy` fallback이 제대로 작동 안함

### 🟡 Important

#### 문제 4: best_model 선택 기준 허점
- **위치**: `model_manager.py:258-296`
- **원인**: `hit_rate=0.0`인 모델도 best로 선택 가능

#### 문제 5: 학습 완료 판정 기준 모호
- **위치**: `lstm_model.py:247-248`
- **원인**: 학습 루프만 완료되면 `is_trained=True`

#### 문제 6: 앙상블 model_manager 참조 누락
- **위치**: `ensemble_model.py:464-477`
- **원인**: pickle 후 로드 시 `set_model_manager()` 호출 필요

---

## 4. 개선 작업 목록

### Phase 0: 데이터 수집 구조 점검 (신규)

- [ ] **Task 0.0**: 대시보드 포트 점검
  - 프론트엔드: localhost:5173 (Vite dev server)
  - 백엔드 API: localhost:8000 (FastAPI)
  - launcher.py 에서 잘못된 포트로 연결되는지 확인 필요

- [ ] **Task 0.1**: 50시간 prediction_history 수집 구조 점검
  - prediction_history가 어떻게 쌓이는지 확인
  - 실시간 예측 → outcome 업데이트 흐름 검증
  - 서버 재시작 시 데이터 유지 여부 확인

- [x] **Task 0.2**: outcome 업데이트 로직 구현 ✅
  - `job_update_prediction_outcomes()` 작업 추가 (scheduler.py:594-728)
  - 5분마다 실행, 60분 지난 예측의 outcome 업데이트
  - DB에서 실제 가격 데이터 조회하여 hit/miss 판정

### Phase 1: 학습 상태 명확화 (Option D 적용)

- [x] **Task 1.1**: Validation Precision 저장 (Option D-1) ✅
  - `initial_precision`, `initial_recall`, `initial_f1`, `validation_samples` 속성 추가 (base_model.py)
  - `calculate_validation_metrics()` 메서드 추가 (base_model.py:121-188)
  - 모든 모델의 `train()` 메서드에서 validation metrics 계산 호출

- [x] **Task 1.2**: hit_rate 표시 우선순위 로직 (Option D-2) ✅
  - 4단계 fallback 구현 (model_manager.py:get_model_performances)
  - 1순위: memory (prediction_history) → 2순위: db → 3순위: validation → 4순위: training
  - API 응답에 `source` 필드 추가됨

- [ ] **Task 1.3**: 데이터 소스 표시 (Option D-3)
  - API 응답에 `source` 필드 추가
  - "realtime" | "db" | "validation" | "training" 구분
  - 프론트엔드에서 소스 표시 (선택적)

- [x] **Task 1.4**: LSTM/Transformer 로드 실패 처리 ✅
  - .pt 파일 없음 → `is_trained=False` 설정
  - .pt 로드 예외 발생 → `is_trained=False` 설정
  - .scaler 파일 없음/실패 → `is_trained=False` 설정
  - 상세 로깅 추가 (warning/error 레벨)

### Phase 2: 앙상블 가중치 개선

- [x] **Task 2.1**: 초기 가중치 계산 개선 ✅ (Option A)
  - 1순위: realtime precision (prediction_history 50h)
  - 2순위: initial_precision (validation set 기반)
  - `_get_model_precisions()` 메서드에 fallback 로직 추가

- [x] **Task 2.2**: 동적 가중치 조정 ✅ (Option C)
  - `EnsembleModel.refresh_weights()` 메서드 추가
  - `ModelManager.refresh_all_ensemble_weights()` 헬퍼 추가
  - `outcome_update` 작업 후 자동 호출 (5분 주기)

### Phase 3: 모델 선택 강화

- [x] **Task 3.1**: best_model 최소 기준 추가 ✅
  - `settings.MIN_PRECISION_THRESHOLD = 0.30` (30%)
  - `settings.MIN_PREDICTION_SAMPLES = 10`
  - 기준 미달시 경고 로그 + 가장 높은 모델로 fallback
  - ModelManager.get_best_model(), EnsembleModel._select_best_model() 적용

- [x] **Task 3.2**: 모델 상태 모니터링 ✅ (Option A)
  - API 응답에 `training_status` 필드 추가 (models.py)
  - 상태값: "not_started", "training", "completed", "failed"
  - Simple inference 방식: is_trained + last_trained 기반 추론

---

## 5. 작업 진행 상황

| Task | 상태 | 담당 | 완료일 |
|------|------|------|--------|
| 문서 작성 | ✅ 완료 | Claude | 2024-12-29 |
| Target 설정 분석 | ✅ 완료 | Claude | 2024-12-29 |
| Task 0.2 (outcome 업데이트) | ✅ 완료 | Claude | 2024-12-29 |
| Task 1.1 (validation metrics) | ✅ 완료 | Claude | 2024-12-29 |
| Task 1.2 (hit_rate 우선순위) | ✅ 완료 | Claude | 2024-12-29 |
| Task 1.3 | ⏳ 대기 | - | - |
| Task 1.4 (LSTM/Transformer 로드) | ✅ 완료 | Claude | 2024-12-29 |
| Task 2.1 (앙상블 초기 가중치) | ✅ 완료 | Claude | 2024-12-29 |
| Task 2.2 (앙상블 동적 가중치) | ✅ 완료 | Claude | 2024-12-29 |
| Task 3.1 (best_model 최소 기준) | ✅ 완료 | Claude | 2024-12-29 |
| Task 3.2 (학습 상태 모니터링) | ✅ 완료 | Claude | 2024-12-30 |

---

## 부록 A: 스케줄러 활성화 조건

### 스케줄러란?
`NASDAQScheduler`는 APScheduler를 사용하여 백그라운드에서 자동으로 실행되는 작업들을 관리합니다.

### 활성화 조건

스케줄러는 **`NASDAQScheduler.start()`가 호출될 때** 활성화됩니다.

#### 활성화 시점

| 실행 방법 | 스케줄러 활성화 | 설명 |
|-----------|----------------|------|
| `python scripts/run_system.py` | ✅ 활성화 | 전체 시스템 실행 (권장) |
| API 서버만 실행 (`uvicorn`) | ❌ 비활성화 | 스케줄러 별도 시작 필요 |
| `python scripts/train_all_models.py` | ❌ 비활성화 | 일회성 학습 스크립트 |

### 등록된 작업 목록 (7개)

| 작업 ID | 주기 | 시장 시간만 | 설명 |
|---------|------|-------------|------|
| `minute_collect` | 1분 | ✅ | 데이터 수집 + 예측 생성 |
| `hourly_training` | 1시간 | ✅ | 증분 학습 |
| `ticker_update` | 1시간 | ❌ | 활성 티커 목록 갱신 |
| `daily_retrain` | 17:00 ET | ❌ | 일일 전체 재학습 |
| `keep_alive` | 30초 | ❌ | 프로세스 유지 |
| `auto_train` | 1시간 | ❌ | 신규 티커 자동 학습 |
| `outcome_update` | 5분 | ❌ | 예측 결과 업데이트 (신규) |

### 스케줄러 상태 확인

```python
# 스케줄러 상태 조회
scheduler.get_status()

# 반환 예시:
{
    "running": True,
    "jobs": [...],
    "job_stats": {...},
    "market_hours": True
}
```

### 주의사항

1. **시장 시간 체크**: `minute_collect`, `hourly_training`은 ET 기준 09:30-16:00 사이에만 실행
2. **재시작 시 데이터 손실**: `prediction_history`는 메모리에만 저장되므로 서버 재시작 시 초기화됨
3. **outcome_update 작업**: 예측 60분 후 실제 결과를 DB에서 조회하여 hit/miss 판정

---

## 부록 B: 핵심 코드 위치

| 기능 | 파일 | 라인 |
|------|------|------|
| 학습 트레이너 | `src/trainer/gpu_trainer.py` | 전체 |
| 모델 매니저 | `src/models/model_manager.py` | 전체 |
| 앙상블 모델 | `src/models/ensemble_model.py` | 전체 |
| LSTM 모델 | `src/models/lstm_model.py` | 전체 |
| Transformer 모델 | `src/models/transformer_model.py` | 전체 |
| 베이스 모델 | `src/models/base_model.py` | 전체 |
| 레이블 생성 | `src/processor/label_generator.py` | 전체 |
| 설정 | `config/settings.py` | 전체 |
| 실시간 예측 | `src/predictor/realtime_predictor.py` | 전체 |
| API 라우트 | `src/api/routes/models.py` | 전체 |
