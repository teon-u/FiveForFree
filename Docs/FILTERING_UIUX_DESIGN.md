# 필터링 기능 강화 UI/UX 설계서

**작성일**: 2025-12-21
**작성자**: 분석팀장
**버전**: v1.0

---

## 1. 현재 상태 분석

### 1.1 기존 필터링 기능

| 기능 | 위치 | 설명 |
|------|------|------|
| 방향 필터 | SettingsPanel | all/up/down 3가지 옵션 |
| 확률 임계값 | SettingsPanel | 0-90% 슬라이더 |
| 카테고리 토글 | Dashboard | Gainers/Volume 전환 |

### 1.2 현재 한계점

1. **섹터/산업 필터 부재**: 기술주, 금융주 등 섹터별 필터링 불가
2. **정렬 옵션 제한**: Precision, Signal Rate 기준 정렬 없음
3. **필터 UI 분산**: 설정 패널 내 깊숙이 위치하여 접근성 낮음
4. **복합 필터 미지원**: 여러 조건 조합 필터링 어려움

---

## 2. UI/UX 개선 설계

### 2.1 필터 바 컴포넌트 (FilterBar.jsx)

대시보드 상단에 상시 노출되는 필터 바 추가

```
┌─────────────────────────────────────────────────────────────────────┐
│ ┌─────────┐  ┌─────────┐  ┌────────────┐  ┌──────────┐  ┌────────┐ │
│ │ 📈 상승 │  │ 📉 하락 │  │ 🏢 섹터 ▼ │  │ 확률 70%▼│  │ 정렬 ▼ │ │
│ └─────────┘  └─────────┘  └────────────┘  └──────────┘  └────────┘ │
│                                                                      │
│ 활성 필터: 기술주(32) | 확률 ≥70% | ✕ 필터 초기화                   │
└─────────────────────────────────────────────────────────────────────┘
```

**구성 요소:**

1. **방향 필터 버튼**: 토글 형식, 다중 선택 가능
2. **섹터 드롭다운**: 체크박스 다중 선택
3. **확률 드롭다운**: 프리셋 선택 + 커스텀 슬라이더
4. **정렬 드롭다운**: 여러 기준 선택
5. **활성 필터 표시줄**: 현재 적용된 필터 태그 표시

### 2.2 섹터/산업 필터 상세

#### 2.2.1 섹터 분류 체계

```
Technology (기술)
├── Software
├── Hardware
├── Semiconductors
└── Internet Services

Healthcare (헬스케어)
├── Biotechnology
├── Pharmaceuticals
└── Medical Devices

Finance (금융)
├── Banks
├── Insurance
└── Fintech

Consumer (소비재)
├── E-commerce
├── Retail
└── Consumer Goods

Energy (에너지)
├── Oil & Gas
├── Renewable
└── Utilities

Industrial (산업재)
├── Manufacturing
├── Transportation
└── Construction
```

#### 2.2.2 섹터 드롭다운 UI

```
┌─────────────────────────────┐
│ 🏢 섹터 선택         [검색] │
├─────────────────────────────┤
│ ☑ 전체 선택                 │
├─────────────────────────────┤
│ ▼ Technology (32)           │
│   ☑ Software (15)           │
│   ☑ Semiconductors (10)     │
│   ☐ Hardware (7)            │
├─────────────────────────────┤
│ ▶ Healthcare (18)           │
│ ▶ Finance (25)              │
│ ▶ Consumer (21)             │
│ ▶ Energy (12)               │
│ ▶ Industrial (14)           │
├─────────────────────────────┤
│        [적용]  [취소]       │
└─────────────────────────────┘
```

**특징:**
- 아코디언 방식으로 하위 산업 펼침/접기
- 각 항목 옆에 티커 수 표시
- 상위 섹터 선택 시 하위 산업 전체 선택
- 검색창으로 빠른 섹터/산업 찾기

### 2.3 확률 임계값 필터

#### 2.3.1 프리셋 + 커스텀 UI

```
┌─────────────────────────────┐
│ 📊 확률 필터                │
├─────────────────────────────┤
│ ● 전체 (0%+)                │
│ ○ High (70%+)               │
│ ○ Very High (80%+)          │
│ ○ Extreme (90%+)            │
├─────────────────────────────┤
│ ○ 커스텀                    │
│ ┌───────────────────────┐   │
│ │ ●─────────────○       │   │
│ └───────────────────────┘   │
│ 최소: 65%      최대: 100%   │
├─────────────────────────────┤
│        [적용]  [취소]       │
└─────────────────────────────┘
```

**특징:**
- 빠른 선택을 위한 프리셋 4가지
- 커스텀 선택 시 범위 슬라이더 활성화
- 범위 선택 가능 (예: 70%~85%)

### 2.4 정렬 옵션

#### 2.4.1 정렬 드롭다운 UI

```
┌─────────────────────────────┐
│ 🔄 정렬 기준                │
├─────────────────────────────┤
│ ● 확률 높은 순 ▼            │
│ ○ 확률 낮은 순 ▲            │
├─────────────────────────────┤
│ ○ Precision 높은 순 ▼       │
│ ○ Precision 낮은 순 ▲       │
├─────────────────────────────┤
│ ○ Signal Rate 높은 순 ▼     │
│ ○ Signal Rate 낮은 순 ▲     │
├─────────────────────────────┤
│ ○ 등급순 (A→D)              │
│ ○ 변동률 높은 순            │
│ ○ 티커명 (A→Z)              │
└─────────────────────────────┘
```

**정렬 옵션 상세:**

| 옵션 | 설명 | 사용 시나리오 |
|------|------|--------------|
| 확률순 | 모델 예측 확률 기준 | 신호 강도 우선 |
| Precision순 | 적중률 기준 | 신뢰도 우선 |
| Signal Rate순 | 신호 발생률 기준 | 활발한 예측 우선 |
| 등급순 | 실용성 등급 A→D | 종합 판단 우선 |
| 변동률순 | 일일 가격 변동 | 모멘텀 우선 |
| 티커명순 | 알파벳 순서 | 특정 종목 탐색 |

---

## 3. 컴포넌트 설계

### 3.1 새로운 컴포넌트 구조

```
frontend/src/
├── components/
│   ├── filters/
│   │   ├── FilterBar.jsx          # 메인 필터 바
│   │   ├── DirectionFilter.jsx    # 방향 필터 (상승/하락)
│   │   ├── SectorFilter.jsx       # 섹터/산업 필터
│   │   ├── ProbabilityFilter.jsx  # 확률 필터
│   │   ├── SortDropdown.jsx       # 정렬 드롭다운
│   │   └── ActiveFilters.jsx      # 활성 필터 태그
│   └── ...
├── stores/
│   └── filterStore.js             # 필터 상태 관리 (신규)
└── data/
    └── sectors.js                 # 섹터/산업 매핑 데이터
```

### 3.2 filterStore.js 설계

```javascript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

export const useFilterStore = create(
  persist(
    (set, get) => ({
      // 방향 필터
      directions: ['up', 'down'], // 선택된 방향들

      // 섹터 필터
      selectedSectors: [], // 선택된 섹터 코드 배열
      selectedIndustries: [], // 선택된 산업 코드 배열

      // 확률 필터
      probabilityPreset: 'all', // 'all', 'high', 'veryHigh', 'extreme', 'custom'
      probabilityRange: { min: 0, max: 100 },

      // 정렬
      sortBy: 'probability', // 정렬 기준
      sortOrder: 'desc', // 'asc' | 'desc'

      // Actions
      setDirections: (dirs) => set({ directions: dirs }),
      toggleDirection: (dir) => set((state) => ({
        directions: state.directions.includes(dir)
          ? state.directions.filter(d => d !== dir)
          : [...state.directions, dir]
      })),

      setSectors: (sectors) => set({ selectedSectors: sectors }),
      setIndustries: (industries) => set({ selectedIndustries: industries }),

      setProbabilityPreset: (preset) => {
        const presets = {
          all: { min: 0, max: 100 },
          high: { min: 70, max: 100 },
          veryHigh: { min: 80, max: 100 },
          extreme: { min: 90, max: 100 },
        }
        set({
          probabilityPreset: preset,
          probabilityRange: presets[preset] || { min: 0, max: 100 }
        })
      },
      setProbabilityRange: (range) => set({
        probabilityPreset: 'custom',
        probabilityRange: range
      }),

      setSortBy: (field) => set({ sortBy: field }),
      setSortOrder: (order) => set({ sortOrder: order }),

      // 필터 초기화
      resetFilters: () => set({
        directions: ['up', 'down'],
        selectedSectors: [],
        selectedIndustries: [],
        probabilityPreset: 'all',
        probabilityRange: { min: 0, max: 100 },
        sortBy: 'probability',
        sortOrder: 'desc',
      }),

      // 활성 필터 개수
      getActiveFilterCount: () => {
        const state = get()
        let count = 0
        if (state.directions.length < 2) count++
        if (state.selectedSectors.length > 0) count++
        if (state.selectedIndustries.length > 0) count++
        if (state.probabilityPreset !== 'all') count++
        return count
      }
    }),
    {
      name: 'nasdaq-predictor-filters',
      version: 1,
    }
  )
)
```

### 3.3 섹터 매핑 데이터 (sectors.js)

```javascript
export const SECTORS = {
  technology: {
    name: { ko: '기술', en: 'Technology' },
    icon: '💻',
    industries: {
      software: { name: { ko: '소프트웨어', en: 'Software' } },
      semiconductors: { name: { ko: '반도체', en: 'Semiconductors' } },
      hardware: { name: { ko: '하드웨어', en: 'Hardware' } },
      internet: { name: { ko: '인터넷', en: 'Internet Services' } },
    }
  },
  healthcare: {
    name: { ko: '헬스케어', en: 'Healthcare' },
    icon: '🏥',
    industries: {
      biotech: { name: { ko: '바이오테크', en: 'Biotechnology' } },
      pharma: { name: { ko: '제약', en: 'Pharmaceuticals' } },
      devices: { name: { ko: '의료기기', en: 'Medical Devices' } },
    }
  },
  finance: {
    name: { ko: '금융', en: 'Finance' },
    icon: '🏦',
    industries: {
      banks: { name: { ko: '은행', en: 'Banks' } },
      insurance: { name: { ko: '보험', en: 'Insurance' } },
      fintech: { name: { ko: '핀테크', en: 'Fintech' } },
    }
  },
  consumer: {
    name: { ko: '소비재', en: 'Consumer' },
    icon: '🛒',
    industries: {
      ecommerce: { name: { ko: '이커머스', en: 'E-commerce' } },
      retail: { name: { ko: '소매', en: 'Retail' } },
      goods: { name: { ko: '소비재', en: 'Consumer Goods' } },
    }
  },
  energy: {
    name: { ko: '에너지', en: 'Energy' },
    icon: '⚡',
    industries: {
      oil: { name: { ko: '석유/가스', en: 'Oil & Gas' } },
      renewable: { name: { ko: '신재생', en: 'Renewable' } },
      utilities: { name: { ko: '유틸리티', en: 'Utilities' } },
    }
  },
  industrial: {
    name: { ko: '산업재', en: 'Industrial' },
    icon: '🏭',
    industries: {
      manufacturing: { name: { ko: '제조', en: 'Manufacturing' } },
      transportation: { name: { ko: '운송', en: 'Transportation' } },
      construction: { name: { ko: '건설', en: 'Construction' } },
    }
  },
}

// 티커-섹터 매핑 (예시, 실제로는 API에서 받아옴)
export const TICKER_SECTORS = {
  AAPL: { sector: 'technology', industry: 'hardware' },
  MSFT: { sector: 'technology', industry: 'software' },
  NVDA: { sector: 'technology', industry: 'semiconductors' },
  GOOGL: { sector: 'technology', industry: 'internet' },
  AMZN: { sector: 'consumer', industry: 'ecommerce' },
  JPM: { sector: 'finance', industry: 'banks' },
  JNJ: { sector: 'healthcare', industry: 'pharma' },
  XOM: { sector: 'energy', industry: 'oil' },
  // ... 추가 매핑
}
```

---

## 4. 인터랙션 설계

### 4.1 필터 적용 플로우

```
[사용자 필터 변경]
        ↓
[filterStore 업데이트]
        ↓
[Dashboard에서 필터링 로직 실행]
        ↓
[TickerGrid 리렌더링]
        ↓
[활성 필터 표시줄 업데이트]
```

### 4.2 UX 가이드라인

| 원칙 | 설명 |
|------|------|
| 즉시 반영 | 필터 변경 시 즉시 결과에 반영 (확인 버튼 불필요) |
| 시각적 피드백 | 활성 필터는 하이라이트, 필터된 개수 표시 |
| 쉬운 초기화 | 한 번의 클릭으로 모든 필터 초기화 |
| 상태 유지 | 페이지 새로고침 후에도 필터 설정 유지 |
| 접근성 | 키보드 네비게이션, 스크린 리더 지원 |

### 4.3 반응형 디자인

#### 데스크톱 (1024px+)

```
┌──────────────────────────────────────────────────────┐
│ [상승] [하락] | [섹터▼] | [확률▼] | [정렬▼] | 초기화 │
└──────────────────────────────────────────────────────┘
```

#### 태블릿 (768px - 1023px)

```
┌──────────────────────────────┐
│ [상승] [하락] | [필터▼] [정렬▼] │
└──────────────────────────────┘
```
필터 버튼 클릭 시 섹터+확률 통합 패널 표시

#### 모바일 (< 768px)

```
┌─────────────────┐
│ [🔍 필터 (3)]    │
└─────────────────┘
```
필터 버튼 클릭 시 풀스크린 필터 모달

---

## 5. 구현 우선순위

### Phase 1: 기본 필터 바 (1주)
- [ ] FilterBar.jsx 컴포넌트 생성
- [ ] DirectionFilter.jsx (기존 기능 이전)
- [ ] filterStore.js 생성
- [ ] Dashboard.jsx 통합

### Phase 2: 정렬 기능 (3일)
- [ ] SortDropdown.jsx 컴포넌트
- [ ] 정렬 로직 구현
- [ ] 다국어 지원

### Phase 3: 섹터 필터 (1주)
- [ ] sectors.js 데이터 파일
- [ ] SectorFilter.jsx 컴포넌트
- [ ] API에 섹터 정보 추가 (백엔드 연동)
- [ ] 섹터별 티커 매핑

### Phase 4: 확률 필터 개선 (3일)
- [ ] ProbabilityFilter.jsx 컴포넌트
- [ ] 프리셋 + 커스텀 범위 UI
- [ ] 범위 슬라이더 구현

### Phase 5: 반응형 최적화 (3일)
- [ ] 모바일 풀스크린 필터 모달
- [ ] 태블릿 레이아웃 조정
- [ ] 터치 제스처 지원

---

## 6. 백엔드 요구사항

### 6.1 API 확장

#### GET /api/tickers 응답 확장

```json
{
  "tickers": [
    {
      "symbol": "AAPL",
      "name": "Apple Inc.",
      "sector": "technology",
      "industry": "hardware",
      "sector_name": { "ko": "기술", "en": "Technology" },
      "industry_name": { "ko": "하드웨어", "en": "Hardware" }
    }
  ]
}
```

#### GET /api/sectors (신규)

```json
{
  "sectors": [
    {
      "code": "technology",
      "name": { "ko": "기술", "en": "Technology" },
      "icon": "💻",
      "ticker_count": 32,
      "industries": [
        {
          "code": "software",
          "name": { "ko": "소프트웨어", "en": "Software" },
          "ticker_count": 15
        }
      ]
    }
  ]
}
```

---

## 7. 테스트 시나리오

### 7.1 기능 테스트

| 시나리오 | 예상 결과 |
|----------|----------|
| 상승만 필터 | 상승 예측 티커만 표시 |
| 기술+금융 섹터 필터 | 해당 섹터 티커만 표시 |
| 확률 80%+ 필터 | 80% 이상 티커만 표시 |
| Precision순 정렬 | Precision 내림차순 정렬 |
| 모든 필터 초기화 | 전체 티커 표시, 기본 정렬 |

### 7.2 성능 테스트

- 100개 티커 필터링: < 50ms
- 필터 변경 UI 반응: < 100ms
- 드롭다운 열기/닫기: < 50ms

---

*이 문서는 분석팀장이 작성하였습니다.*
