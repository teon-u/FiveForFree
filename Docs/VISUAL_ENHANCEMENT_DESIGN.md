# 시각적 개선 설계서

**작성일**: 2025-12-21
**작성자**: 분석팀장
**버전**: v1.0

---

## 1. 개요

### 1.1 목적

티커 카드의 **시각적 차별화**를 통해 사용자가 한눈에 예측 품질과 등급을 파악할 수 있도록 개선

### 1.2 현재 상태

```javascript
// TickerCard.jsx - 현재 스타일
const getCardStyle = () => {
  if (probability >= 80) {
    return direction === 'up' ? 'strong-up' : 'strong-down'
  } else if (probability >= 70) {
    return direction === 'up' ? 'up' : 'down'
  }
  return 'neutral'
}
```

**현재 문제점:**
- 확률 기반 스타일만 적용
- Practicality Grade(실용성 등급) 시각적 구분 부족
- 카드 테두리 색상 없음 (등급 배지만 존재)
- 중요 정보(A등급 기회) 시선 유도 어려움

---

## 2. 등급별 테두리 색상 가이드

### 2.1 색상 체계

| 등급 | 테두리 색상 | Hex 코드 | 배경 Glow | 사용 시나리오 |
|------|-------------|----------|-----------|--------------|
| A | 골드/그린 | `#22c55e` | `shadow-green-500/30` | 최고 품질 예측 |
| B | 블루 | `#3b82f6` | `shadow-blue-500/20` | 양호한 예측 |
| C | 옐로우 | `#eab308` | `shadow-yellow-500/15` | 보통 예측 |
| D | 레드/그레이 | `#6b7280` | `shadow-gray-500/10` | 참고용 예측 |
| N/A | 회색 점선 | `#374151` | 없음 | 데이터 부족 |

### 2.2 시각적 계층 구조

```
┌─────────────────────────────────────────────────────────────────┐
│ A등급                                                           │
│ ┌───────────────────────────────────────────────────────────┐  │
│ │ ███ 골드/그린 테두리 (2px solid) + 배경 Glow              │  │
│ │     + 펄스 애니메이션 (선택적)                             │  │
│ └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│ B등급                                                           │
│ ┌───────────────────────────────────────────────────────────┐  │
│ │ ███ 블루 테두리 (2px solid) + 은은한 Glow                 │  │
│ └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│ C등급                                                           │
│ ┌───────────────────────────────────────────────────────────┐  │
│ │ ░░░ 옐로우 테두리 (1px solid) + 미세 Glow                 │  │
│ └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│ D등급                                                           │
│ ┌───────────────────────────────────────────────────────────┐  │
│ │     회색 테두리 (1px solid) + 글로우 없음                  │  │
│ └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. CSS 구현

### 3.1 Tailwind CSS 클래스

```css
/* index.css 또는 globals.css에 추가 */

/* Grade A - 최고 품질 */
.grade-border-a {
  @apply border-2 border-green-500 shadow-lg shadow-green-500/30;
}

.grade-border-a-glow {
  @apply ring-2 ring-green-400/50 ring-offset-2 ring-offset-surface;
}

/* Grade B - 양호 */
.grade-border-b {
  @apply border-2 border-blue-500 shadow-md shadow-blue-500/20;
}

/* Grade C - 보통 */
.grade-border-c {
  @apply border border-yellow-500/70 shadow-sm shadow-yellow-500/15;
}

/* Grade D - 참고용 */
.grade-border-d {
  @apply border border-gray-600;
}

/* N/A - 데이터 부족 */
.grade-border-na {
  @apply border border-dashed border-gray-700;
}
```

### 3.2 애니메이션 정의

```css
/* A등급 펄스 애니메이션 (선택적) */
@keyframes grade-pulse {
  0%, 100% {
    box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.4);
  }
  50% {
    box-shadow: 0 0 0 8px rgba(34, 197, 94, 0);
  }
}

.grade-a-animate {
  animation: grade-pulse 2s ease-in-out infinite;
}

/* 호버 효과 */
.grade-border-a:hover {
  @apply shadow-xl shadow-green-500/40 scale-[1.02] transition-all duration-200;
}

.grade-border-b:hover {
  @apply shadow-lg shadow-blue-500/30 scale-[1.02] transition-all duration-200;
}
```

### 3.3 다크 모드 지원

```css
/* 다크 모드에서 더 밝은 테두리 */
.dark .grade-border-a {
  @apply border-green-400;
}

.dark .grade-border-b {
  @apply border-blue-400;
}

.dark .grade-border-c {
  @apply border-yellow-400/80;
}
```

---

## 4. 컴포넌트 수정

### 4.1 TickerCard.jsx 수정안

```jsx
// TickerCard.jsx

export default function TickerCard({ prediction, onClick, onDetailClick }) {
  const { practicality_grade, probability, direction } = prediction

  // 등급 기반 테두리 스타일
  const getGradeBorderClass = (grade) => {
    switch (grade) {
      case 'A':
        return 'grade-border-a grade-a-animate'
      case 'B':
        return 'grade-border-b'
      case 'C':
        return 'grade-border-c'
      case 'D':
        return 'grade-border-d'
      default:
        return 'grade-border-na'
    }
  }

  // 방향 기반 배경 그라데이션 (보조적)
  const getDirectionBgClass = () => {
    if (probability >= 70) {
      return direction === 'up'
        ? 'bg-gradient-to-br from-green-900/20 to-surface'
        : 'bg-gradient-to-br from-red-900/20 to-surface'
    }
    return 'bg-surface'
  }

  return (
    <div
      className={clsx(
        'ticker-card relative rounded-xl p-4 transition-all duration-200',
        getGradeBorderClass(practicality_grade),
        getDirectionBgClass()
      )}
    >
      {/* 카드 내용... */}
    </div>
  )
}
```

### 4.2 등급 배지 개선

```jsx
// 기존 배지 스타일 업그레이드
const getGradeBadgeStyle = (grade) => {
  const styles = {
    A: {
      bg: 'bg-green-500/20',
      border: 'border-green-500',
      text: 'text-green-400',
      icon: '⭐'  // 별 아이콘 추가
    },
    B: {
      bg: 'bg-blue-500/20',
      border: 'border-blue-500',
      text: 'text-blue-400',
      icon: null
    },
    C: {
      bg: 'bg-yellow-500/20',
      border: 'border-yellow-500',
      text: 'text-yellow-400',
      icon: null
    },
    D: {
      bg: 'bg-red-500/20',
      border: 'border-red-500',
      text: 'text-red-400',
      icon: null
    }
  }
  return styles[grade] || styles.D
}

// 배지 렌더링
const GradeBadge = ({ grade }) => {
  const style = getGradeBadgeStyle(grade)

  return (
    <span className={clsx(
      'px-2 py-0.5 rounded text-xs font-bold border',
      style.bg, style.border, style.text
    )}>
      {style.icon && <span className="mr-1">{style.icon}</span>}
      {grade}
    </span>
  )
}
```

---

## 5. 시각적 힌트 추가

### 5.1 A등급 하이라이트

```jsx
// A등급 카드에 특별 마크 추가
{practicality_grade === 'A' && (
  <div className="absolute -top-2 -right-2 w-6 h-6 bg-green-500 rounded-full flex items-center justify-center shadow-lg shadow-green-500/50">
    <span className="text-white text-xs font-bold">★</span>
  </div>
)}
```

### 5.2 확률 프로그레스 바 색상

```jsx
// 확률 바 색상도 등급 연동
const getProbBarColor = (probability, grade) => {
  if (grade === 'A') return 'bg-green-500'
  if (grade === 'B') return 'bg-blue-500'
  if (probability >= 80) return 'bg-green-400'
  if (probability >= 70) return 'bg-blue-400'
  return 'bg-gray-500'
}
```

---

## 6. 카드 상태별 스타일

### 6.1 상태 조합 매트릭스

| 등급 | 확률 80%+ | 확률 70-79% | 확률 <70% |
|------|-----------|-------------|-----------|
| A | 골드 테두리 + 강한 Glow + 펄스 | 골드 테두리 + 중간 Glow | 골드 테두리 + 약한 Glow |
| B | 블루 테두리 + 강한 Glow | 블루 테두리 + 중간 Glow | 블루 테두리만 |
| C | 옐로우 테두리 + 미세 Glow | 옐로우 테두리만 | 옐로우 테두리 (투명도 낮춤) |
| D | 회색 테두리 | 회색 테두리 | 회색 테두리 (점선) |

### 6.2 복합 스타일 함수

```javascript
const getCompositeCardStyle = (grade, probability, direction) => {
  const baseClass = getGradeBorderClass(grade)

  // 확률에 따른 강조 레벨
  let intensityClass = ''
  if (probability >= 80) {
    intensityClass = 'ring-2 ring-current/30'
  } else if (probability >= 70) {
    intensityClass = 'ring-1 ring-current/20'
  }

  // 방향 배경
  let bgClass = ''
  if (probability >= 70 && direction === 'up') {
    bgClass = 'bg-green-950/30'
  } else if (probability >= 70 && direction === 'down') {
    bgClass = 'bg-red-950/30'
  }

  return clsx(baseClass, intensityClass, bgClass)
}
```

---

## 7. 접근성 고려사항

### 7.1 색맹 대응

| 등급 | 색상 | 패턴/아이콘 | 텍스트 라벨 |
|------|------|-------------|-------------|
| A | 녹색 | ★ (별) | "최고" |
| B | 파랑 | 없음 | "양호" |
| C | 노랑 | 없음 | "보통" |
| D | 회색 | 없음 | "참고" |

### 7.2 ARIA 라벨

```jsx
<div
  role="article"
  aria-label={`${ticker} 티커, 등급 ${grade}, 예측 확률 ${probability}%`}
  className={getCompositeCardStyle(grade, probability, direction)}
>
  ...
</div>
```

### 7.3 고대비 모드

```css
@media (prefers-contrast: high) {
  .grade-border-a {
    @apply border-4 border-green-300;
  }
  .grade-border-b {
    @apply border-4 border-blue-300;
  }
  .grade-border-c {
    @apply border-4 border-yellow-300;
  }
  .grade-border-d {
    @apply border-4 border-gray-300;
  }
}
```

---

## 8. 성능 최적화

### 8.1 애니메이션 최적화

```css
/* GPU 가속 활성화 */
.grade-a-animate {
  will-change: box-shadow;
  transform: translateZ(0);
}

/* 리듀스 모션 지원 */
@media (prefers-reduced-motion: reduce) {
  .grade-a-animate {
    animation: none;
  }
}
```

### 8.2 조건부 렌더링

```jsx
// 성능: 애니메이션은 A등급만 적용
const shouldAnimate = practicality_grade === 'A' && !prefersReducedMotion
```

---

## 9. 비교 화면

### 9.1 Before (현재)

```
┌─────────────────────┐  ┌─────────────────────┐
│ NVDA          🟢    │  │ AAPL          🟢    │
│    82% ↑            │  │    75% ↑            │
│ Model: XGB    [A]   │  │ Model: LGBM   [B]   │
│ (모든 카드 동일)     │  │ (모든 카드 동일)     │
└─────────────────────┘  └─────────────────────┘
```

### 9.2 After (개선 후)

```
╔═══════════════════════╗  ┌─────────────────────┐
║ ★ NVDA         🟢    ║  │ AAPL          🟢    │
║    82% ↑      ✨     ║  │    75% ↑            │
║ Model: XGB    [A⭐]  ║  │ Model: LGBM   [B]   │
║ (골드 테두리+Glow)   ║  │ (블루 테두리)        │
╚═══════════════════════╝  └─────────────────────┘
```

---

## 10. 구현 일정

### Phase 1: CSS 기반 작업 (1일)
- [ ] index.css에 등급별 테두리 클래스 추가
- [ ] 애니메이션 키프레임 정의
- [ ] 호버 효과 구현

### Phase 2: 컴포넌트 수정 (1일)
- [ ] TickerCard.jsx 등급 스타일 적용
- [ ] 등급 배지 개선
- [ ] A등급 하이라이트 마크

### Phase 3: 접근성 및 테스트 (반나절)
- [ ] ARIA 라벨 추가
- [ ] 색맹 대응 패턴 적용
- [ ] 리듀스 모션 테스트

---

## 11. 테스트 체크리스트

| 항목 | 확인 |
|------|------|
| A등급 카드 골드 테두리 표시 | [ ] |
| B등급 카드 블루 테두리 표시 | [ ] |
| C등급 카드 옐로우 테두리 표시 | [ ] |
| D등급 카드 회색 테두리 표시 | [ ] |
| A등급 펄스 애니메이션 작동 | [ ] |
| 호버 시 확대 효과 | [ ] |
| 다크 모드 색상 적절 | [ ] |
| 리듀스 모션 시 애니메이션 비활성화 | [ ] |
| 스크린 리더 등급 안내 | [ ] |

---

*이 문서는 분석팀장이 작성하였습니다.*
